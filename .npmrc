# NPM 配置优化 - 加速依赖安装

# 使用国内镜像源（可选，根据网络环境选择）
# registry=https://registry.npmmirror.com

# 安装优化
prefer-offline=true
fetch-retries=3
fetch-retry-mintimeout=10000
fetch-retry-maxtimeout=60000

# 减少日志输出
loglevel=error

# 禁用进度条（CI 环境推荐）
progress=false

# 严格模式（保证依赖版本一致）
engine-strict=true

# 缓存配置
cache-min=3600

# 构建优化
# 禁用可选依赖（如果不需要）
# optional=false
